apiVersion: v1
kind: Service
metadata:
  name: llm-multi-agent-frontend
spec:
  selector:
    app: llm-multi-agent-frontend
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: ClusterIP
